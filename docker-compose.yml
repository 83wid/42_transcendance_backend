version: '3.3'

services:
  db:
    image: postgres
    container_name: db
    restart: always
    volumes:
      - ./database/setup.sql:/docker-entrypoint-initdb.d/setup.sql
    env_file:
      - .env

  adminer:
    depends_on:
      - db
    image: adminer
    container_name: adminer
    restart: always
    ports:
      - 8080:8080